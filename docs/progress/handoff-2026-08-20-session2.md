# ハンドオフ 2026-08-20（第2セッション）— 読解ページの本番公開とテストデータの片付け

## 3行でいうと

- **読解ページを本番に公開した。**生徒が実際に開けることを本番で確認済み（塾長承認・憲法4）
- 確認に使った**テスト生徒の作業データを消した。実際にあったのは5件**（指示書の記載は6件）。実在の生徒の行は**もともと1件も無い**
- 作ったものは何も変えていない。公開と片付けだけの回

## やったこと

### 1. 送り出す前の目視確認

未送信15件の中身を1本ずつ確認した。内訳は指示書のとおり（読解13本＋規程の写し削除1本＋教材データ1本）で、想定外のものは無かった。あわせて次も確認した。

- AI へ問い合わせる仕組みの下ごしらえが混ざっていないこと（**第2弾は不承認のまま**。該当なし＝追加費用0円を維持）
- 合言葉や鍵の類が紛れていないこと（該当なし）
- 新しく増える置き場所は `024_reading_progress.sql` の1つだけで、これは前回すでに適用済み

### 2. 送り出し（本番公開）

送る前にこのパソコンで、テスト507件すべて通過・本番用の組み立ても成功を確認してから送った（`c1348a5` → `03a56d4`）。
自動配信が新しい版に入れ替わったことを、読解の受け口が「ログインしてください」を返すこと（=その受け口が存在すること）で確認した。

### 3. 生徒の画面が本番で開けることの確認

テスト生徒で本番（`https://srs.swallow-base.com`）にログインし、スマホ幅で確認した。

- 読解の一覧に**第2〜7講の6講が並ぶ**
- 講に入ると本文が出て、「切る作業を始める」まで進める
- 途中保存が効いており、前回の続き（第2講のまとめ画面）から開けた

### 4. テスト生徒の作業データの片付け

消す前に全行を一覧して、**5件すべてがテスト生徒（確認用）のもので、実在の生徒の行が1件も無い**ことを確認してから消した。

| 消したもの | 5件（第2・3・4・6・7講） |
|---|---|
| 消さなかったもの | 無し（テーブル全体が0件になった＝実在の生徒の行は元々無い） |
| 指示書との差 | 指示書は6件。**第5講は保存が残っていなかった**ため実際は5件 |

消したあと、テスト生徒で読解の一覧を開き直しても**再び保存され直さない**ことを確認した（0件のまま）。
講師画面「読解の取組状況」も本番で開き、「まだ読解に取り組んだ生徒はいません。」と出ることを確認した。

## 確認した環境

- **本番（`https://srs.swallow-base.com`）＋本番のデータベース**。生徒は「テスト生徒（確認用）」「テスト講師（確認用）」のみ
- 画面の確認は Playwright のデバイス再現（iPhone 13 / iPad Mini）
- 単体テストと組み立ての確認は、このパソコン（送り出す直前）

## 残っていること

1. **実在の講師アカウントでの講師画面の目視**。テスト生徒はクラスに属さないため一覧に出ない（仕様どおり）。実際の生徒が取り組んだあとに確認するのが確実
2. **正規形（AI採点の基準）は全講とも塾長の検収待ち。**届いていなくても動く作りのまま
3. 第2弾（アプリの中で AI 講評を回す）は**不承認のまま**。呼び出し口の下ごしらえも作っていない
