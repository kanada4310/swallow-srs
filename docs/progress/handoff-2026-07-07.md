# ハンドオフ 2026-07-07

> 単語帳（定着度ビュー）＋古文単語演習の表示改善／デザイン全面刷新／チュートリアル第1弾。

### 2026-07-07（古文単語演習の表示改善＋単語帳「定着度ビュー」）✅コミット済み・要実機確認
- **依頼3点**: ①モードA で漢字（読み仮名）が答えを露呈 → 出題中は隠す ②A/B の解答表示で「正解を1つだけ選んだ」時に選んだ正解へ「あなたの解答」しか出ず誤答と誤解 → 両タグ表示 ③間違えを蓄積して苦手単語を一覧化、全単語を定着度アイコン付きで表示・定着度で絞り込み（タゲ友「マイ単語帳」イメージ）
- **合意（AskUserQuestion）**: ③の対象範囲=**全デッキ共通の汎用版**／入口=ユーザー提案どおり**デッキ詳細のノート一覧に統合**（別ページ化しない）
- **①②（`KobunTangoCard.tsx`）**: ①モードA の出題ステムから `{{漢字}}` 表示を削除（採点後フィードバックの `見出し語（漢字）` は従来どおり＝学習効果保持）。②`optTag` を単一→複数タグ返却に変更。isChosen で「あなたの解答」（正解なら中立色 `you-ok`／誤答なら赤 `you`）、isCorrect で「正しい選択肢」（選択済みは緑 `right`／未選択は緑枠 `right-line`）を**それぞれ**出す。`.kt-tags` flex コンテナ＋`.you-ok` スタイル追加。A/B 共通
- **③ 単語帳（定着度ビュー）**:
  - 純ロジック `src/lib/wordbook/mastery.ts`（テスト13件）: `deriveMastery(card_states)` → 5段階 **未学習/苦手/学習中/定着中/定着済み**。しきい値=学習中<7日≤定着中<21日≤定着済み（実効 stability＝FSRS stability or SM-2 interval）。**苦手**＝relearning／learning での失敗／lapses≥2 かつ未固定（eff<21）を拾う（=間違えの蓄積の可視化）。`aggregateMastery`（1単語が複数カード時は一番弱いレベル採用）・`summarizeMastery`・`MASTERY_ORDER`（苦手=最優先）
  - データ `src/lib/wordbook/wordbook-data.ts`: `getWordbookForDeck(deckId, userId)`＝デッキ＋子孫の全カード×ユーザー card_states を **見出し語（無ければ note 単位）でグループ化**して WordbookEntry[] を返す。Dexie のみ・オフライン可（庭 garden-data と同じ思想・`pickLabel` 再利用）。**純表示層で学習エンジン不変**
  - UI `src/components/deck/WordbookView.tsx`: `useLiveQuery`。定着度フィルタチップ（件数バッジ＋色）＋検索＋苦手が上の並び。単語タップ→`/study?deck=X&card=cardId`（既存 priorityCardId で当該カードを最優先復習）。100件ずつ「もっと見る」
  - 統合 `DeckDetailClient.tsx`: ノート一覧セクションに**タブ切替**「ノート一覧」⇄「単語帳（定着度）」を追加。`useAuth()` の userId を渡す（＝ログイン中ユーザー自身のマイ単語帳）。生徒・講師とも表示
- **検証**: tsc/lint クリーン／テスト**410件**（+13）／本番ビルド成功（exit 0・`/decks/[id]` 241kB）。**`Array.from(map.values())` 必須**（`--downlevelIteration` 既知の罠を再び踏んだ→修正済み）
- **コミット**: `f31a66c`（main へ push 済み）
- **次セッション注意**:
  - **アプリ改修なので要 Vercel デプロイ＋再ログイン**（DB変更・マイグレーション無し）。デプロイ後 `/decks` → 古文単語315 デッキ詳細で確認
  - 定着度は**ログイン中ユーザー自身**の card_states 由来（講師が生徒の定着度を見るのは別機能 `/students/progress`）。card_states 未同期だと全て「未学習」表示
  - しきい値（`mastery.ts` の `MASTERY_THRESHOLDS`=stable7/mastered21・`WEAK_LAPSES`=2）は実データで要調整
  - 単語帳の「補足(sub)」は汎用フィールド（意味/和訳/Back/漢字…）から拾うため、**古文モードA は漢字が sub に出る**（語義は 問題 JSON 内で未抽出＝インライン意味表示は将来強化余地）。中学英単語など「意味」フィールドがあるデッキでは意味が出る
  - 大規模デッキ（中学英単語6858＝グループ後~2286語）でも全件走査＝タブを開いた時のみ実行。重ければ将来キャッシュ/仮想化

### 2026-07-07（デザイン全面刷新「藍・空・喉の橙」＋チュートリアル第1弾）
- **依頼の流れ**: ①学習の待ち時間問題の相談 → SRS全体の総合評価（4観点並列レビュー・改善レポート提示）②デザインが高校生に響かない → ブラウザ実機確認＋モックアップ提案 → 方向性承認 ③「全画面を新デザインに・カード様式の世界観は独立のまま・メインカラー/アイコン変更OK」＋チュートリアル設計承認
- **デザインシステム**: `docs/design-system.md` 新規（トークン・慣用句・置換マップ・禁止事項）。tailwind.config に ai/sora/nodo/paper/ink＋評価4色＋rounded-card/shadow-card。**全ページ＋全共有コンポーネントを並列エージェントで再スキン**（学習画面・ホーム・完了画面・デッキ一覧/詳細・ノート・統計・設定・認証・講師画面・モーダル/エディタ類）。カード内部様式（テンプレCSS/KobunTango/MultiStep/庭）は不変。theme_color=#1C2B4B。InstallPrompt を /study で抑止
- **生徒ホーム刷新**: `getTodayMissionLocal`（ルートデッキ毎の new_cards_per_day 残枠＋期限復習から**今日の実枚数**を算出）→ミッションカード＋開始CTA（残数最多デッキへ）＋🔥ストリークチップ。旧「新規カード=未学習全件数」表示を廃止
- **チュートリアル第1弾**: デッキ「つばめSRSのつかいかた」15枚投入済み（id `12347515-3c06-4c44-872b-e20e160328ea`・**未配布**・`data/create-tutorial-deck.mjs --reset` で再投入可）＋コーチマーク（初回フリップ時に評価ボタン説明・localStorage 既読）
- **エンジン修正**: `getStudyCardsOffline` の deckCards を created_at→id で安定ソート（「順番どおり」が本当に作成順に。従来はUUID順=実質ランダム）
- **検証**: tsc/lint クリーン・テスト397件・build 成功。Chrome実機でデッキ一覧/学習/完了/チュートリアル出題順/コーチマークを確認
- **追記（同日後半）**: ①**生徒スマホ環境の再現ツール** `scripts/device-preview.mjs`（Playwright devDep＋chromium。`SRS_AUTH_SECRET` で JWT 署名→本物の `/auth/line` 経路で「テスト生徒（確認用）」に自動ログイン・iPhone/iPad エミュレーション・`.device-preview/shots/` にスクショ・永続プロファイルで IndexedDB 保持。**Git Bash からは `MSYS_NO_PATHCONV=1` 必須**。`--teacher` は偽講師が teacher-shared 同期に載るので通常使わない）。テスト生徒 `dd608ec4…` にはチュートリアルデッキを個人配布済み＝ホームのミッションカード「5枚/約3分」実機確認OK ②**PWAアイコン新調** `data/generate-pwa-icons.mjs`（sharp・藍グラデ×白つばめ×橙点・8サイズ上書き）③BottomNav「生徒」を lucide Users に ④デッキ詳細のツールボタンを sora-soft チップに（無色で視認性が悪かった）
- **次セッション注意**:
  - **要 Vercel デプロイ＋再ログイン**。スマホ確認は `scripts/device-preview.mjs` でいつでも可能
  - ダークモードはトークン差し替えで対応可能
  - チュートリアルデッキは**旧id `dc7dcb60…` を一度削除して再投入**済み。金田のブラウザの IndexedDB に旧デッキのゴーストが残る可能性（再ログインで消える）
  - 総合評価レポート（同日前半）の未着手項目: **learn-ahead（待ち時間解消・最優先）**、練習モードの記録方式、速度→簡単判定の重み、同期の差分化、送信キューのデータ消失対策、StudySession/schema.ts の分割リファクタ
  - 荒井先生など他講師の端末も再同期でデザイン反映（データ変更はチュートリアルデッキのみ）
