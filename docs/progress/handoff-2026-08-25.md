# ハンドオフ 2026-08-25: スパルタプログラム管理

指示書: 統合塾運営エージェント `docs/work-orders/2026-08-25-srs-sparta-program-management.md`（塾長承認済みで着手）

## できたこと

- **スパルタプログラム管理を実装**（登録・自動進捗集計・終了サマリー・生徒ホーム表示）
  - 新テーブル `sparta_programs`（`supabase/migrations/026_sparta_programs.sql`）— **本番未適用・塾長承認待ち**
  - 純ロジック `src/lib/sparta/logic.ts`（進捗導出・日本時間4時区切り・入力検査。テスト23件）
  - サーバー集計 `src/lib/sparta/server.ts`（review-pause の DeckMaps 再利用・service role＋担当生徒チェック）
  - 講師画面 `/students/sparta`＋API `/api/teacher/sparta`（登録・編集・中止・削除・一覧・終了サマリー）
  - 生徒ホームに `SpartaCard`（`/api/sparta/mine`）
  - ADR `docs/decisions/20260825-sparta-program-management.md`（設計判断6件の理由つき）
- **催促の確認**: 既存のLINE学習リマインダー（due-cards-summary）は全ルートデッキの期限切れを集計するため、
  スパルタ対象デッキの未学習分も自動で朝の通知に乗る（SRS側の変更なし・契約C19の形式は不変）

## 検証（実行環境: ローカル・モックのみ。本番/実DBでの動作確認は未実施）

- テスト: **564件全通過**（既存541＋新規23）
- 型チェック: 新規エラーなし（既知の `src/lib/db/sync.test.ts` 1件のみ・本作業と無関係）
- Lint: 新規警告なし / `next build` 成功（`/students/sparta` を含む）
- **画面の実動作は未確認**: `sparta_programs` 表が本番DBに無いため、ローカルでもAPIは空/エラー応答になる。
  026適用後に本番 smoke（講師画面で登録→一覧表示→生徒ホーム表示）が必要

## 本番反映の結果（2026-08-25・塾長承認済み＝選択肢1）

- **git push 済み**（Vercel 自動デプロイ・本番 /login 応答200を確認）
- **026 は未適用**: このセッションのツール制限（自動許可の分類器）で `npx supabase db push` と
  本番ログインを伴う画面確認が実行できなかった（dry-run で対象が026のみであることは確認済み）。
  適用までの間、講師の新画面はデータ取得エラー表示・生徒ホームのスパルタカードは非表示（既存機能への影響なし）

## 次にやること

1. **塾長の作業**: swallow-srs で `npx supabase db push` を1回実行（026適用。承認済みの操作）
2. **適用後の smoke**: 講師画面「生徒取組状況」→「スパルタの管理」で ①登録（生徒・デッキ・期間・目標）
   ②一覧に進捗が出る ③対象生徒のホームにスパルタカードが出る、を確認
3. **起案（billing側・別指示書）**: スパルタ特有の催促。(a) 未着手の生徒（対象デッキの card_states がまだ無い
   ＝期限切れが発生しない）には既存リマインダーが飛ばない (b) 期限接近・「今日まだ」の文言はFlex変更が必要
4. しきい値の実データ調整（習得基準の既定 stable=7日が実運用に合うか、最初のプログラムで確認）

## 注意

- `sparta_programs.deck_ids` はFKなしの配列（デッキ削除に耐える）。表示側で「(削除されたデッキ)」と出る
- baseline（登録時習得数）は登録APIが計算して保存。対象デッキ/習得基準を編集すると測り直す
- 日付キーは日本時間の午前4時区切り（`spartaDayKey`）。既存の一部集計（サーバーローカル4時）と方式が異なる
