# ハンドオフ 2026-08-26: ペン下線ずれ修正＋テストデータの後片付け

指示書: 統合塾運営エージェント `docs/work-orders/2026-08-26-srs-pen-underline-fix.md`

## やったこと

1. **下線ずれの修正**（既知の課題に登録済みだった1件）
   - 症状: 同じ行の左側にカッコがあると、右側の単語の下線がカッコの幅ぶん左にずれる
   - 原因: 単語の箱の採寸（`PenSyntaxAnnotator` の `measure`）が「文の変更」「枠サイズの変更」でしか
     走らず、カッコ記号の挿入（正解表示・書き込み）で単語が右へ押されても測り直していなかった
   - 修正: 解答のまとまり・開きかけの括弧・採点対象外マーク・採点マークが変わったときにも
     採寸をやり直す effect を追加（`PenSyntaxAnnotator.tsx`）
   - 回帰テスト: `PenSyntaxAnnotator.test.tsx` 新設（模擬レイアウトで「カッコ挿入→下線が
     押された後の位置に来る」を検証。修正を外すと失敗することも確認済み）
   - 動作確認: 開発サーバーでテスト生徒ログイン→構文の練習→例文②「The girl standing by the door…」の
     正解表示で、デスクトップ幅・スマホ幅（375px・折り返し2行）とも全下線が単語の箱と一致
2. **スパルタのテスト登録1件を削除**（塾長承認済みの後片付け）
   - 削除した行: `sparta_programs` id `9356d101-79d7-4580-88a0-7ecd8a7b6276`
     （生徒「テスト生徒（確認用）」= line_device-preview-student / 期間 2026-08-25〜2026-09-21 /
     対象デッキ「古文単語315」1件 / **状態は canceled**＝指示書の前提「active」と異なっていたが、
     生徒・期間・デッキ数が一致するテスト登録そのものなので削除）
   - **発見**: 別アカウント「テスト生徒」（line_Udcd03859…・実LINE連携）に同期間の **active な
     スパルタ登録がもう1件残っている**（id `9364ef7f-a692-40f8-baea-0bbd251cd265`）。
     指示書の削除許可は device-preview-student の1件のみなので触っていない。質問ファイルで塾長に確認
3. **テスト生徒の読解途中保存1件を削除**: `reading_progress`
   （user=テスト生徒（確認用）/ lesson_id=英語長文最前線_第2講 / 切る段階・区切りなし）
   削除後、テスト生徒の `sparta_programs`・`reading_progress` とも残存0件を確認

## テスト結果

- 623件全緑（従来622＋新規1）。型エラー1件（`sync.test.ts`）と Lint 警告1件（`TemplatePreview`）は
  従来からの既知のもので本セッションと無関係

## 本番反映（2026-08-26・塾長承認済み）

- 質問ファイル `ec5ba508-pen-underline-push.md` への回答で **push・追加削除の両方が承認**され、実行済み
- **push 済み・Vercel デプロイ成功**。本番の構文の練習・例文②の正解表示を Playwright（テスト生徒で
  実ログイン）で確認: **iPhone 13 幅（390px・2行折り返し）は下線4本、iPad Mini 幅（768px・1行）は
  下線3本すべてが単語の箱と一致**（座標実測・スクリーンショット取得）
- **追加削除も実行済み**: 「テスト生徒」（実LINE連携・line_Udcd03859…）の active なスパルタ登録
  `sparta_programs` id `9364ef7f-a692-40f8-baea-0bbd251cd265`（期間 2026-08-25〜2026-09-21・
  対象デッキ「つばめSRSのつかいかた」1件・baseline 0）を削除。**削除後、スパルタ登録は全0件**

## やり残し・注意

- なし（指示書の項目はすべて完了）。本番確認中に一度だけ古い Service Worker のキャッシュで
  画面が白くなる事象を見たが、これはデプロイ直後の資材入れ替わりによる一時的なもの
  （再読み込みで解消・アプリの不具合ではない）
