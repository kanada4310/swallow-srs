# ハンドオフ 2026-08-26: 分析の順序の記録と検討順ヒント

指示書: 統合塾運営エージェント `docs/work-orders/2026-08-26-srs-pen-order-hints.md`
ADR: `docs/decisions/20260826-pen-order-hints.md`（slug: pen-order-hints）

## やったこと

1. **手順0（塾長承認済み）**: `.claude/settings.json` を新規作成し、git push の許可を追記
   （`Bash(git push:*)`・`PowerShell(git push:*)`。従来はローカル設定に場当たりの個別許可が
   積まれ、承認済みの push が毎回止まっていた）。規程「push は塾長承認後」は不変
2. **順序の記録**（`src/lib/pen-syntax/order.ts`）: ペン方式の記入・一手戻す・削除を
   時系列でため、採点時に「どの記号をどの順で書いたか」の並びへ畳む（筆画の開始時刻から導出。
   同じマスの上書きは最後の記入だけ残す）。解答・採点データの形式は不変の付帯情報。
   並びの控えは端末内 localStorage（直近30件）。**DB変更なし・外部送信なし**
3. **模範筆順の採取口**: 講師は採点後に「この筆順を模範の順序として保存」→ ページ下部
   「模範の順序（講師用）」一覧から JSON コピー（`ModelOrderPanel.tsx`）。
   公開後に塾長が1〜2文で採取し、報告に貼れば検討順リストの並べ替え材料になる
4. **検討順ヒント**（`src/lib/pen-syntax/order-hints.ts`・規則ベース0円）:
   「💡 迷ったらまずこれ」ボタン。確定済みの記号から「まだの最初の項目」を1つだけ提示。
   初期リスト = ①文の動詞 → ②S → ③名詞の4役 → ④カッコの検討順（［名詞］→｛ ｝→〈 〉→（ ））。
   リストは設定データ（`ORDER_HINT_RULES` の並び）で後から入れ替え可能。
   文面は「次に調べる対象と調べ方（辞書・ルールブックの引き先）」まで＝答えは言わない
   （テストで機械的に担保）。ペン・タップ両方式で使える

## 確認したこと

- テスト **711件全緑**（開始時688件＋新規23件）・`tsc` は既知の1件のみ（sync.test.ts・既存）・
  Lint 警告も既存のみ・**ビルド成功**
- 開発サーバー＋テスト生徒（iPad Mini 幅・Playwright）で通し確認:
  ヒントが 動詞→S と段階遷移／採点で「書いた順序」に「V｜plays」が出る／
  生徒には講師の保存ボタンが出ない（スクリーンショット `.device-preview/shots/verify-*.png`）
- 講師UI（保存ボタン・模範一覧）は role ゲートの表示のみのため実講師アカウントでの
  画面確認は未（塾長の採取作業＝下記がそのまま初回確認になる）
- **本番反映済み（2026-08-26）**: 質問ファイル回答A（塾長承認・秘書回答）のうえ push
  （5108952→6612e46・手順0の許可により権限で止まらず通過）→ Vercel デプロイ成功（success）→
  本番の「構文の練習」でヒントの段階遷移（動詞→S）・採点・「書いた順序」表示を
  テスト生徒ログインの実操作で確認、「ペン判別の計測」の表示も正常（JSエラーなし）。
  確認に使ったダミーお手本（確認用プロファイルの localStorage）は確認後に消去済み

## 塾長への案内（模範筆順の採取）

本番反映後、タブレット＋ペンで本番の「構文の練習」を開き:

1. 問題を1つ選び、**いつもの順序どおり**にペンで分析して「採点する」
2. 採点結果の下「✍️ 書いた順序」で並びを確認 →「この筆順を模範の順序として保存」
3. ページ下部「模範の順序（講師用・この端末に保存）」を開き「コピー」→ そのまま報告に貼る
   （1〜2文ぶんで十分。この並びを見て検討順ヒントの初期リストを調整します）

## やり残し・次段の候補

- 模範筆順と生徒の並びの突き合わせ（採取後・検討順リストの調整とセットで別起案）
- 順序の自動採点・矛盾検査への組み込み／本編1文画面への搭載（指示書のスコープ外のまま）
- 並びの控えのサーバー保存（要るなら DB 変更＝塾長承認事項として別起案）
